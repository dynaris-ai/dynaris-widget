/**
 * Alias for {@link ./vite.config.js}. Use plain `pnpm dev` (runs `vite`).
 * This file exists so `vite --config vite.playground.config.js` still works if copied from old notes.
 */
export { default } from './vite.config.js';
